<div class="container-fluid">
    <div class="row">
        <div class="col-md-2" id="filterBar">
            {{filter-bar
                action="onFilter"
                games=filteredGames
            }}
            <!-- TODO: tags and genres, not games -->
        </div>
        <div class="col-md-10" id="gamesList">
            {{games-list
                games=filteredGames
            }}
        </div>
    </div>
</div>
